const getters = {
  assessData: state => state.assessment.assessData,
  reportArgs: state => state.assessment.reportArgs,
  configIds: state => state.assessment.configIds,
  content: state => state.assessment.content,
}

export default getters