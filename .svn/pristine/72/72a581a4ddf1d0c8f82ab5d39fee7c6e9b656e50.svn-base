const salary = {
	state: {
		approvalUnitNowRecord: '',
		nowApprovalRecord: '',  //工资发放审批 -> 审批 工资科
		historyRecord: {},//历史数据存放
		SalaryData: ''//全单位数据存放
	},
	mutations: {
		changeApprovalUnitNowRecord(state, val) {
			state.approvalUnitNowRecord = val
		},
		CommitNowApprovalRecord(state, val) {
			state.nowApprovalRecord = val
		},
		CommitHistoryRecord(state, val) {
			state.historyRecord = val
		},
		CommitSalaryData(state, val) {
			state.SalaryData = val
		}
	}
}

export default salary