/*报表模板 模板指令的css*/
/* 数据绑定 span标签弃用 */
span[bind], var[bind]{
    background-color: rgba(255, 145, 0, 0.2);;
    cursor: default;
}
var[bind]{
    font-style: unset;
}
span[bind]:hover, var[bind]:hover{
    background-color: rgba(255, 145, 0, 0.4) !important;
}
span[bind]::before, var[bind]::before{
    content: '{';
}
span[bind]::after, var[bind]::after{
    content: '}';
}

/* 表头 */
table thead td,table thead th{
    background-color: rgba(255, 145, 0, 0.1);
}
/* 通过伪元素遮照方案可以不影响表的原样式 但和合并单元格指令的样式冲突
table thead td,table thead th{
    position: relative;
    background: unset;
}
thead td::after,thead th::after{
    content:'';
    background:rgba(255, 145, 0, 0.1);
    position:absolute;
    top:0;
    left:0;
    z-index: -100;
    width:100%;
    height:100%;
} */

/* 循环 */
tr[foreach]{
    outline: 2px dashed rgba(255, 145, 0, 0.8);
    outline-offset: 2px;
}
/* 添加for说明和单元格选中样式的before伪元素冲突 先去掉
tr[foreach]>:first-child{
    position: relative;
}
tr[foreach]>:first-child::before{
    content: 'for';
    background-color: rgba(255, 145, 0, 0.8);
    position: absolute;
    top: -3px;
    left: -3px;
    cursor: default;
    line-height: 1em;
    padding: 2px;
    color: white;
    font-size: 12px;
}*/


/** 单元格合并 */
td[merge-left],td[merge-top],td[merge-right],td[merge-right]{
    position: relative;
}
td[merge-left]::after, td[merge-top]::after, td[merge-right]::after, td[merge-right]::after{
    content: 'M';
    background-color: rgba(255, 145, 0, 0.8);
    position: absolute;
    right: 0;
    bottom: 0;
    cursor: default;
    line-height: 1em;
    padding: 1px;
    color: white;
    font-size: 12px;
}