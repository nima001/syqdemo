const getters = {
  // fields: state => state.query.fields,
  // fieldstwo: state => state.query.fieldstwo,
  // tilefields: state => state.query.tilefields,
  // treeData: state => state.query.treeData
}
export default getters
