<template>
  <div class="exiting">
    <a-icon type="loading"/>正在登出，请稍后…
  </div>
</template>
<script>
import { Spin, Icon } from "ant-design-vue";
import { showError } from "@/framework/utils/index";
import { logoutZfw } from '../api/login'

export default {
  name: "ZfwExit",
  components: {
    ASpin: Spin,
    AIcon: Icon,
  },
  created() {
    logoutZfw().finally(() => {
      let { href } = this.$router.resolve('/idmgzmh/sso/login');
      (window.top || window).location.replace(href);
    })
  },
};
</script>
<style lang="less" scoped>
.exiting{
  position: absolute;
  top: 50%;
  width: 100%;
  margin-top: -15px;
  height: 30px;
  line-height: 30px;
  text-align: center;
  font-size: 16px;
  .anticon{
    margin-right: 10px;
    color: @primary-color;
  }
}
</style>